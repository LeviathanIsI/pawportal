<%- include("header") %>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Home</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/users/signup">Sign Up</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/sessions/login">Log In</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto"> <!-- This ul is pushed to the right -->
                    <li class="nav-item">
                        <p class="nav-link" id="welcome">Welcome, <%= currentUser.userName %>
                        </p>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <%- include("footer") %>